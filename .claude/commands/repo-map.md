# /repo-map

Produce a repo map focused on "what runs where" for the SOC testbed.

Output:
- Major directories and purpose
- Entry points (scripts, configs)
- Verification commands (local-only)
- VM-dependent steps (explicitly labeled)
