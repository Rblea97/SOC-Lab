# Claude Code Workflow

See `.claude.md` at repo root for the canonical session contract:
gates, edit boundaries, forbidden actions, and one-task workflow.

## Quick reference

- Run all gates: `uv run nox -s all`
- Run pre-commit: `pre-commit run --all-files`
- Extended commands: `.claude/commands/`
