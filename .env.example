# Example environment file (safe placeholders only)
#
# Copy to .env and fill in real values locally:
#   cp .env.example .env

# Generic examples (adjust to your environment)
SOC_LAB_ENV=dev

# If any scripts/tools support API keys or webhooks, keep real values in .env only.
API_TOKEN=CHANGEME
